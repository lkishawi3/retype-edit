// Test file for ReType overlay functionality
// Start practice mode and try typing these lines to verify:
// 1. Original text is never modified
// 2. Incorrect characters stop progress and highlight in red
// 3. Cursor only advances on correct input

function testFunction() {
    const message = "Hello World!";
    console.log(message);
    return message.length;
}

// Try typing this line incorrectly first, then correctly:
const numbers = [1, 2, 3, 4, 5];

// Test special characters and symbols:
if (numbers.length > 0) {
    console.log("Array has elements");
} 